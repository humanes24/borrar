version: 2
project_name: oda-lite

release:
  prerelease: false
  draft: false
  extra_files:
    - glob: dist/**/*.tar.gz
    - glob: dist/**/*.sha256

changelog:
  sort: desc
  use: github

# No builds aquí; adjuntamos ficheros ya generados mediante release.extra_files
